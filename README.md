# 👟 Sneakers Bot

Этот бот автоматически отслеживает скидки на мужские кроссовки на сайте и отправляет уведомления с **фотографиями** в Telegram канал.

## 🛠 Как это работает

1.  **Планировщик (`main.py`)**:
    *   Каждые 30 минут запускает процесс проверки.
    *   Также можно запустить проверку вручную командой `/latest`.

2.  **Парсер (`scraper.py`)**:
    *   Запускает скрытый браузер Google Chrome (Headless mode).
    *   Заходит на страницу распродажи.
    *   Считывает "сырые" данные с сайта (из `window.__NUXT__`), что позволяет получать точные цены и ссылки на изображения высокого качества.

3.  **База данных (`database.py`)**:
    *   Каждый найденный товар проверяется в файле `deals.db`.
    *   Если ссылка на товар уже есть в базе — он пропускается (чтобы не спамить).
    *   Если товара нет — он сохраняется как "отправленный".

4.  **Telegram (`main.py`)**:
    *   Новые товары отправляются в настроенный канал (например, `@krosi_na_skidochkax`).
    *   Сообщение содержит **фотографию кроссовок**, цену, старую цену, размер скидки и кнопку "Купить".

## 🚀 Запуск

1.  Установите зависимости:
    ```bash
    pip install -r requirements.txt
    ```
2.  Запустите бота:
    ```bash
    python main.py
    ```

## ⚙️ Настройки (`config.py`)
*   `CHANNEL_ID`: ID или юзернейм канала для рассылки.
*   `TARGET_URL`: Ссылка на раздел магазина, который мониторим.
*   `BOT_TOKEN`: Токен от @BotFather.
